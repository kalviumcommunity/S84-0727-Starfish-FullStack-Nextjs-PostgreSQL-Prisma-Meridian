# Development Guide

## Prerequisites

Before running the project locally, make sure the required development tools are installed.

Typical requirements include:

- Node.js
- npm
- PostgreSQL

## Installation

Install project dependencies using:

```bash
npm install